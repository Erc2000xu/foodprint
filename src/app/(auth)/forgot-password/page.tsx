import Link from "next/link"; import { requestReset } from "../actions";
export default async function ForgotPassword({ searchParams }: { searchParams: Promise<{ error?: string; sent?: string }> }) { const p = await searchParams; return <main className="auth-page"><section className="auth-card"><p className="eyebrow">找回密码</p><h1>重设登录密码</h1>{p.error && <p className="form-error">{p.error}</p>}{p.sent ? <p className="form-success">如果该邮箱存在，我们已发送重设链接。</p> : <form action={requestReset}><label>邮箱<input name="email" type="email" required autoComplete="email" /></label><button className="primary-button">发送重设链接</button></form>}<Link href="/login">返回登录</Link></section></main>; }
