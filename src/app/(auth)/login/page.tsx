import Link from "next/link";
import { signIn } from "../actions";
export default async function Login({ searchParams }: { searchParams: Promise<{ error?: string; reset?: string }> }) { const p = await searchParams; return <main className="auth-page"><section className="auth-card"><p className="eyebrow">食迹 Foodprint</p><h1>欢迎回来</h1>{p.error && <p className="form-error">{p.error}</p>}{p.reset && <p className="form-success">密码已更新，请登录。</p>}<form action={signIn}><label>邮箱<input name="email" type="email" required autoComplete="email" /></label><label>密码<input name="password" type="password" required minLength={8} autoComplete="current-password" /></label><button className="primary-button">登录</button></form><Link href="/forgot-password">忘记密码？</Link><p className="auth-note">食迹仅限受邀成员使用。</p></section></main>; }
