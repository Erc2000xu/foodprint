# 食迹 Foodprint｜视觉资产台账

> 建立日期：2026-07-29  
> 最后更新：2026-08-18
> 用途：在产品开发前统一确认需要生成、绘制、替换和复用的视觉资产，避免编码阶段临时使用 emoji 或风格不一致的占位图形。  
> 维护原则：任何新页面进入开发前，先检查本台账；新增视觉资产必须先登记、出方向稿、确认，再进入制作和接入。

## 1. 为什么需要这份台账

食迹同时包含三类视觉内容，不能用同一种方法处理：

1. **品牌型图像**：吉祥物、空状态、成功页插画、页面氛围图。适合先用 ImageGen 做方向探索，再保存项目内正式资产。
2. **产品语义图标**：三级推荐、“好在哪儿”、地点层级等。必须成组设计、语义稳定、跨页面复用；不能由每个页面临时画一套。
3. **功能型图标**：搜索、返回、关闭、定位、导航、爱心、展开等。默认使用受控 SVG 图标组件；产品明确批准材质化方向时，可以使用已登记、真实透明、完成小尺寸 QA 的 PNG srcset，并保留确定性 SVG 回退。无论哪种格式，都必须保证小尺寸清晰、状态和无障碍不依赖图片本身。

emoji 只能出现在用户输入的内容，或经过产品明确批准的轻松文案中；不得作为正式功能按钮、评分、地点分类和导航图标的默认实现。

## 2. 状态标准

每项资产按以下阶段推进：

`待定义 → 方向稿 → 已确认 → 已制作 → 已接入 → 已验收`

另外使用两个特殊状态：

- `待迭代`：已有资产正在使用，但风格、清晰度或语义需要重做。
- `保留`：已有资产符合当前方向，本轮不修改。

每项必须记录：资产编号、页面/场景、语义、制作方式、尺寸与变体、当前状态、文件位置、最后确认版本。

## 3. 品牌基础

| 编号 | 资产 | 使用场景 | 制作方式 | 当前状态 | 现有文件/备注 |
| --- | --- | --- | --- | --- | --- |
| BR-01 | 食迹应用图标与腊肠犬主形象 | 启动图标、品牌识别 | 品牌插画 | 保留，未来统一审查 | `public/mascot/icon-192.png`、`icon-512.png`、`apple-touch-icon.png` |
| BR-02 | 品牌色与材质板 | 全产品 | 设计令牌，不生成位图 | 已确认基础方向 | 米白、浅鼠尾草绿、深青绿、少量橙红 |
| BR-03 | 路线/地图辅助纹样 | 背景、分隔、引导 | SVG/CSS | 待定义 | 只作为 03 路线的辅助语言，不抢主内容 |
| BR-04 | 字体层级示例 | 标题、正文、数字、少量手写感 | 字体规范 | 待定义 | 手写感只用于少量情绪标题，不用于功能文字 |

## 4. 核心产品语义图标

| 编号 | 图标组 | 数量 | 使用场景 | 制作方式 | 状态 | 文件/下一步 |
| --- | --- | ---: | --- | --- | --- | --- |
| IC-01 | 三级小碗奖章 | 3 | 发现、地图、地点详情、记一顿、饭后聊 | ImageGen 方向 + 透明 PNG 组件 | 已接入，待移动端验收 | `public/icons/recommendation/` |
| IC-02 | 好在哪儿 | 4 | 吃得香、坐得住、聊得开、花得值 | ImageGen D 方向 + 独立透明 PNG 母稿与 256px UI 文件 | 已制作，待接入 | `public/icons/good-at/`；2026-07-30 确认用于 V1.3.3 全局 |
| IC-03 | 地点层级 | 3 | 市级、区级、商圈 | 成组定制线性图标或徽记 | 待定义 | 缺少商圈数据时整项隐藏 |
| IC-04 | 推荐菜 | 1 | 卡片推荐菜预览、详情菜品 | 定制小图标 | 待定义 | 与发现卡完整视觉稿一起确认 |
| IC-05 | 候选/去试试 | 1 | 添加候选、候选身份标签 | 定制小图标 | 待定义 | 语义应是“先记下想法”，不能像普通收藏 |
| IC-06 | 导航去这里 | 1 | 去试试卡片、地点详情 | 标准 SVG + 品牌化容器 | 已接入，待验收 | `src/components/try/try-list.tsx`；16px 纸飞机式路线箭头、文字“导航去这里”，打开高德第三方导航 |
| IC-07 | 下回吃爱心 | 2 状态 | 发现卡、个人收藏 | 标准 SVG | 待迭代 | 未收藏青绿描边；已收藏红色实心 |
| IC-08 | V2.3 地图 Pin 与聚合 | 三级默认 / 选中 + 聚合 default / active + 用户位置 | 发现页动态地图 | 五套 ImageGen 方向探索；选定后用确定性 SVG / CSS 正式重绘 | 已接入代码，待真实地图 / 真机验收 | 2026-08-13 选定 B 餐盘定位圆章；`DynamicMapAdapter` / `MarkerCluster` 已使用正式资产 `public/icons/map-pins/`；规范 `docs/design/v2-3-map-pins/final/README.md`；禁止把方向 PNG 用于运行时 |

### 4.1 IC-08｜V2.3 正式地图 Pin 资产清单

产品负责人于 2026-08-13 从 A–E 五套方向中确认 B「餐盘定位圆章」。ImageGen 方向图只作为选择记录；生产版本为专门针对地图小尺寸重绘的确定性 SVG。

| 语义 | 默认资产 | 选中 / 激活资产 |
| --- | --- | --- |
| 1 级「值得去」/ 一层小碗 | `public/icons/map-pins/pin-level-1-default.svg` | `public/icons/map-pins/pin-level-1-selected.svg` |
| 2 级「想再去」/ 两层小碗 | `public/icons/map-pins/pin-level-2-default.svg` | `public/icons/map-pins/pin-level-2-selected.svg` |
| 3 级「会专门去」/ 三层小碗 | `public/icons/map-pins/pin-level-3-default.svg` | `public/icons/map-pins/pin-level-3-selected.svg` |
| 聚合 | `public/icons/map-pins/cluster-default.svg` | `public/icons/map-pins/cluster-active.svg` |
| 用户当前位置 | `public/icons/map-pins/user-location.svg` | 不适用 |

- 默认单点 40 × 45px；选中 48 × 54px；聚合 44 × 50px / 激活 48 × 54px；命中区至少 44 × 44px。
- 单点 / 聚合统一以底部中心 `(0.5, 1)` 锚定经纬度；用户位置用独立蓝色体系。
- 选中态同时改变尺寸与橙红轮廓；聚合数字由 DOM 动态显示 1–99 / 100+；按下、焦点、减少动态效果由统一 CSS 提供。
- `public/icons/map-pins/manifest.json` 是机器清单；`src/lib/amap/map-pin-assets.ts` 与 `map-pin-elements.ts` 是运行入口；页面不得散写文件路径。
- 小尺寸和模拟地图 QA 位于 `docs/design/v2-3-map-pins/final/qa/`；真实高德多底图、320 / 390 / 430px 和真机 QA 完成后再升级为“已接入 / 已验收”。

### 4.2 IC-02｜“好在哪儿”正式资产清单

产品负责人于 2026-07-30 确认 D“温暖餐桌物件”为正式方向。不得继续使用 C 版页面意向图或五套方向板中自动生成的占位图标。

| slug | 中文 | 母稿 | 程序版 |
| --- | --- | --- | --- |
| `tasty` | 吃得香 | `public/icons/good-at/tasty-master.png` | `public/icons/good-at/tasty-ui.png` |
| `comfortable` | 坐得住 | `public/icons/good-at/comfortable-master.png` | `public/icons/good-at/comfortable-ui.png` |
| `good_for_chat` | 聊得开 | `public/icons/good-at/good-for-chat-master.png` | `public/icons/good-at/good-for-chat-ui.png` |
| `good_value` | 花得值 | `public/icons/good-at/good-value-master.png` | `public/icons/good-at/good-value-ui.png` |

- 母稿：1254 × 1254、透明 RGBA PNG，用于后续重新导出，不由页面直接加载。
- 程序版：256 × 256、透明 RGBA PNG，是 V1.3.3 页面唯一允许调用的文件。
- 视觉：深青绿描边、暖米白陶瓷/纸张、金色与少量橙色点缀；主体在安全画布内尽量饱满。
- 生成源、五套方向板和小尺寸 QA 保存在 `docs/design/v1-3-3-good-at-icons/`。
- 全局范围：凡展示四维评价，均通过同一 `GoodAtIcon` 组件调用；包括记一顿首次/再次到访、发现卡、地点详情、饭后聊，以及后续新增的四维汇总。
- 数据库和服务端只保存/返回四个 slug，不保存图片路径。
- 深青实心底会降低椅架对比度；选中态使用浅鼠尾草底、深青边框和勾选标记。

### 4.3 V2.4｜地图 UI 演示稿的资产边界

2026-08-16 的 V2.4 地图 UI 演示稿已用于确认空间、材质、层级、面板状态和交互，但不是新的视觉资产包。开发只能把它作为布局与交互参考，不得从演示 HTML、截图或对话中提取 / 重画以下正式资产：

| 类别 | V2.4 必须沿用 | 演示稿中不得接入生产的内容 |
| --- | --- | --- |
| 品牌 | BR-01：`public/mascot/icon-192.png` 与现有 Header 品牌组件 | emoji 犬形、临时 Logo、演示文字组合 |
| 地图点位 | IC-08：`public/icons/map-pins/` 正式 SVG，经 `map-pin-assets.ts` / `map-pin-elements.ts` 调用 | CSS 圆点、餐具符号、临时颜色、默认高德蓝 Pin |
| 底部导航 | NAV-01：`public/nav-icons/*.png`、现有标签 / 顺序 / 路由 | 演示底栏的 Unicode、emoji、临时图形或新排序 |
| 地点照片 | 私有真实照片组件与正式无照片状态 | 演示照片、公开 POI 图、伪造餐食图 |
| 地图底图 | 高德 JS API 正式底图与经真机验收的官方样式 | 演示稿的 CSS 道路、示意配色、假 Logo / 版权标识 |
| 字体 / 功能图标 | 当前字体令牌与登记过的受控 SVG / 组件 | 演示字体、emoji 功能图标、未登记位图 |

本轮批准的新视觉语言仅限：顶部安全留白、暖色半透明叠层、轻柔阴影、紧凑搜索 / 筛选、地图与底部面板的纵深关系，以及三种语义面板状态。若以后确需替换表中正式资产，必须另立资产编号、方向确认和接入验收，不能把“演示稿已通过”视为资产批准。

## 5. 吉祥物与场景插画

| 编号 | 资产 | 页面/状态 | 状态 | 现有文件/下一步 |
| --- | --- | --- | --- | --- |
| MS-01 | 欢迎插画 | 登录/首次进入 | 待迭代 | `public/mascot/welcome.jpg`，需核对是否符合 01 主路线 |
| MS-02 | 空地图插画 | 发现无数据 | 待迭代 | `public/mascot/empty-map.jpg` |
| MS-03 | 记录成功插画 | 记一顿成功 | 保留，未来统一审查 | `public/mascot/mark-success.jpg` |
| MS-04 | 离线插画 | 无网络 | 保留，未来统一审查 | `public/mascot/offline.jpg` |
| MS-05 | 无照片占位 | 发现卡、去试试卡 | 待定义 | 不能伪造食物照片；可使用极简品牌占位 |
| MS-06 | 去试试空状态 | 候选列表为空 | 待定义 | 可表现腊肠犬拿着小纸条/路线卡，不进入每张卡 |
| MS-07 | 去试试转正成功 | 完成一顿并进入发现 | 待定义 | 应与记一顿成功场景共用或形成同系列 |
| MS-08 | 搜索无结果/高德暂不可用 | 搜索反馈 | 待定义 | 优先轻量图形，不使用大插画挤占页面 |

## 6. 导航和通用功能图标

以下项目默认采用统一 SVG 组件库，不逐个使用 ImageGen，但仍需在视觉验收中成组检查线宽、圆角、尺寸和状态。UI-02 的 V2.4.1 定位按钮是已登记例外：产品明确要求保留软陶材质，因此使用 ImageGen 正式母稿导出的透明 PNG srcset，并保留 SVG 回退。

| 编号 | 图标组 | 状态 | 备注 |
| --- | --- | --- | --- |
| NAV-01 | 底部五项导航：发现、去试试、记一顿、饭后聊、我的 | 待迭代 | 当前为 `public/nav-icons/*.png`；卡片视觉冻结后整组审查 |
| UI-01 | 搜索、清除、返回、关闭、展开/收起 | 待统一 | 16–24px，需有键盘与无障碍状态 |
| UI-02 | 列表/地图、定位、当前位置、距离 | 定位按钮正式透明 PNG 套件与 SVG 回退已接入，待真机验收 | V2.4.1 定位按钮：`public/icons/map-controls/locate-current-{26,52,78}.png`；manifest：`public/icons/map-controls/locate-current.manifest.json`；资产合同：`docs/design/v2-4-1-location-control/README.md`；其余列表 / 地图图标仍待统一 |
| UI-03 | 添加、编辑、管理菜单、下架/恢复、删除/隐藏、上传照片 | 待统一 | V1.3.3 使用标准 SVG 与文字；普通卡片显示 `···` 管理菜单，不生成垃圾桶位图；危险操作不能只靠颜色表达 |
| UI-04 | 高德导航、查看地图、外部跳转 | 待统一 | 必须明确会打开第三方地图 |
| UI-05 | 成功、警告、网络错误、隐私提示 | 待统一 | 图标不能替代完整状态文字 |

### 6.1 UI-02｜V2.4.1 定位按钮

- 2026-08-18 使用内置 ImageGen 参考现有导航资产生成“深青定位环 + 暖米白内盘 + 橙红中心点”正式母稿，并用确定性脚本完成真实透明抠图、去紫边、裁边、居中和多尺寸导出。
- 第一版 `imagegen-direction-location-control.png` 把棋盘格画进 RGB 像素，只作失败记录；第二版 `imagegen-chroma-master-v2.png` 是抠图处理源。两者均不得上线。
- 正式运行时资产为 `public/icons/map-controls/locate-current-26.png`、`-52.png`、`-78.png` 的 1×/2×/3× RGBA srcset；1024px 母稿和 256px 通用程序版保存在同一目录。
- `public/icons/map-controls/locate-current.svg` 保留为确定性回退；正式路径、尺寸、SHA-256 和 alpha 审计记录在 `locate-current.manifest.json`。
- 五张 PNG 已验证为 8-bit RGBA，四角 alpha 为 0，可见洋红残留为 0；QA 检查板为 `docs/design/v2-4-1-location-control/qa-location-control.png`。
- 该图标表示“获取当前位置并按距离排序”，不能替换蓝色用户位置 Marker，也不能和 IC-08 Foodprint 地点 Pin 混用。
- 按钮默认、定位中、距离排序生效和失败状态由统一组件 / CSS 与可访问文案表达，不重复生成四套状态位图。

## 7. 按页面检查

| 页面 | 需要的品牌/语义视觉 | 当前优先级 |
| --- | --- | --- |
| 发现 | 三级小碗、四项评价、地图 Pin / 聚合 / 选中态、地点层级、推荐菜、爱心、无照片占位、筛选图标 | P0，V2.3 动态地图 |
| 去试试 | 候选标识、导航、商圈、定位、空状态、转正成功 | P0，V1.3.2 |
| 记一顿 | 三级小碗、四项评价、照片上传、定位、成功插画 | P0，V1.3.x |
| 饭后聊 | 三级小碗、评价维度、时间线节点 | P1 |
| 地点详情 | 地点层级、导航、相册、推荐菜、评价维度 | P1 |
| 我的 | 个人记录、下回吃、邀请、成员、导出、安装提示 | P1 |
| 登录/邀请 | 欢迎插画、成功/失效状态 | P2 |
| 离线/PWA | 离线插画、更新与安装图标 | P2 |

## 8. 开发门禁

1. 新需求评审时先登记资产编号，不允许直接在代码里决定图标。
2. 方向稿可由 ImageGen 辅助，功能型小图标优先使用 SVG；若产品明确确认带材质和轻立体感的位图路线，必须像 IC-01/IC-02 一样登记透明母稿、程序版、组件和适用范围。最终资产必须保存到项目，而不是只存在于对话或临时目录。
3. 已确认资产必须提供统一组件，页面只能调用组件，不能复制图片路径或自行换图。
4. 每个图标至少检查 320、375、390px 手机宽度以及 1×/2×/3× 屏幕缩放。
5. 每个非装饰图标必须配文字、可读标签或无障碍名称；不能只靠颜色区分状态。
6. 替换资产时保留版本记录，先更新台账，再更新代码；历史数据不得因为视觉升级无法显示。
7. 每次生成、重做或确认 UI/品牌图片后，当轮必须更新本台账中的状态、正式路径、尺寸/变体、适用页面、确认版本和下一步；不得只把文件放进项目而不登记。
8. 对话截图、可点击演示、竞品截图和临时 HTML 中的占位 Logo、Pin、导航、照片、字体与底图默认不具备资产批准效力；开发必须回到本台账和正式组件查找真相源。

## 9. 已保留的视觉方向

- `docs/design/v1-3-2-try/` 保存三套“去试试”方向稿。
- 产品负责人更偏向 B“出发地图”；该偏好登记为 V3 视觉系统输入。
- V1.3.2 不进行整体视觉翻新，只实施功能所需的局部图标、状态和布局调整。
