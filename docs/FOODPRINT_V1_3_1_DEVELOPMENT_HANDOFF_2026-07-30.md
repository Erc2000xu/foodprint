# 食迹 Foodprint｜V1.3.1 开发交付

> 日期：2026-07-30
> 分支：`codex/v1-3-record-a-meal`
> 状态：代码开发完成，待 Preview / 数据库 / Edge Function 发布与人工验收

## 已交付

1. 发现卡紧凑布局：3:4 竖图、上层“下回吃”、三级推荐文案、全部非零“好在哪儿”、历史“适合”分层、最多两道推荐菜预览。
2. 搜索与筛选：站内词、结果数、可移除地点标签；行政区精确匹配；商圈 3 公里与地铁 1.5 公里半径；高德不可用时保留站内筛选。
3. 单一筛选浮层：一次只开一个，支持点外部、Escape、选择后关闭；受控场景字典不再出现 `afternoon_tea`。
4. 位置一致性：发现卡与详情页共享城市/行政区前缀清理；商圈只读取高德缓存，缺失即隐藏。
5. 商圈补齐：新增可刷新缓存 migration；新地点保存后尝试补齐，发现页每次最多处理三条历史记录；临时失败退避重试，未命中 30 天后再查。
6. 测试运行稳定性：Vitest 固定单线程池，避免此前启动后无结果；migration 测试改用工作区绝对路径。
7. 同步修复高德 Origin 解析对 `https://*.vercel.app` 的错误接受，继续只允许完整精确 Origin。

## 验证证据

- `npm run typecheck`：通过。
- `npm run lint`：通过。
- `npm test`：13 个测试文件、36 项测试全部通过，覆盖卡片、筛选浮层、位置筛选、迁移与既有回归。
- `npm run build`：通过；18 个静态页面生成完成。

完整的负责人验收步骤见 `docs/acceptance/V1_3_1_PREVIEW_ACCEPTANCE_CHECKLIST.md`。

## 发布顺序

1. 先应用 `20260730100000_v1_3_1_amap_business_area_cache.sql`。
2. 再部署更新后的 `amap-poi-search`；确认函数环境存在 `SUPABASE_SERVICE_ROLE_KEY` 与既有 `AMAP_WEBSERVICE_KEY`、Origin 配置。
3. 部署 Preview，使用 320 / 375 / 390px 检查长名称、长地址、四项“好在哪儿”、三个浮层、收藏两种状态和推荐菜收束。
4. 用一个新地点验证即时商圈查询；用历史地点验证每次最多三条补齐、未命中隐藏和临时失败不阻塞发现列表。
5. 项目负责人验收后再合入 `main`；本交付未代替发布批准。

## 回滚

- 前端可直接回滚到上一部署；新增缓存表不影响旧页面读取。
- Edge Function 可回滚到上一版本；缓存数据会保留但不会被旧代码读取。
- migration 为向前兼容的新增表、类型、策略和 RPC，不在紧急回滚中删除；若商圈服务异常，停止部署新函数或关闭调用即可，城市与行政区仍正常展示。

## 下一步唯一动作

创建 V1.3.1 Preview 并完成窄屏与真实高德商圈人工验收；未通过前不开始 V1.3.2 发布工作。
