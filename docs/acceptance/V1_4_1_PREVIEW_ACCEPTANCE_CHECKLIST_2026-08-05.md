# 食迹 Foodprint｜V1.4.1 Preview 人工验收清单

## 1. 验收信息

- 开发状态：项目负责人已完成 Preview 人工验收，无遗留问题。
- 开发分支：`codex/v1-4-1-ui-polish`
- 产品事实基线：[V1.4.1 Spec](../specs/2026-08-v1-4-1-ui-polish.md)
- 上一版本基线：[V1.4 Preview 验收清单](./V1_4_PREVIEW_ACCEPTANCE_CHECKLIST_2026-08-03.md)
- Preview 地址：已由项目负责人检查；本记录不重复保存地址：`____________________________`

项目负责人于 2026-08-05 完成 Preview 检查并确认没有问题；以下人工验收项据此收束为通过。

## 2. 自动检查

在仓库根目录执行：

```bash
npm run typecheck
npm test
npm run lint
npm run build
git diff --check
```

代码交付时填写实际结果：

- `typecheck`：`环境阻塞：TypeScript 在依赖解析阶段长时间无输出，已停止；待 CI/Preview 环境复核`
- `test`：`环境阻塞：Vitest 在启动阶段长时间无输出，已停止；待 CI/Preview 环境复核`
- `lint`：`环境阻塞：ESLint 在启动阶段长时间无输出，已停止；待 CI/Preview 环境复核`
- `build`：`环境阻塞：Next build 在启动阶段长时间无输出，已停止；待 CI/Preview 环境复核`
- `git diff --check`：`通过`
- 静态契约与 TSX 语法解析：`通过；已确认原生按钮/ARIA/本地资源路径/对齐 CSS 契约，PNG 资源 135007 bytes`

上述“环境阻塞”只记录本机工具链未能在合理时间内产出结果，不代表对应检查通过；Preview 或 CI 必须重新执行并回填最终状态。

## 3. 准备数据与角色

- 准备一个 Owner 或 Admin 测试账号，并确保共同地图有可管理地点。
- 准备一个普通 Member 测试账号，确认其不会看到管理按钮。
- `/admin` 中准备至少一条上架地点、一条可恢复的下架地点或候选/隐藏内容，便于检查管理列表按钮。
- 使用浏览器 Responsive Design Mode，分别检查 320、375、390 px。
- 不要直接在生产账号执行下架、恢复、隐藏、退出共同地图或导出操作。

## 4. 发现页 `/`

- [x] Owner/Admin 的地点卡片右上角显示统一的玉白圆牌图形，不再显示放大的文字 `···`。
- [x] 图形在有照片、无照片和浅色背景卡片上保持清晰，约 28 px 可视尺寸，没有绿边、白边异常、裁切或模糊。
- [x] 点击图形仍打开“查看地点详情 / 管理到访记录 / 下架地点”；点击卡片正文不会被图片层拦截。
- [x] Tab 可以到达原生按钮，Enter/Space 可以打开菜单；屏幕阅读器读到 `管理{地点名}`，不读出装饰图片。
- [x] 点击卡片外部关闭菜单；下架确认弹窗、权限控制和反馈保持 V1.4 行为。
- [x] 普通 Member 看不到管理按钮，也不能因为知道图片路径而获得管理能力。
- [x] “下回吃”、搜索按钮、筛选入口和列表/地图切换在进行中、禁用和普通状态下文字均水平/垂直居中，没有跳位。

## 5. 我的页 `/admin`

- [x] “查看 / 管理记录”、恢复、候选管理、安装到设备、下载/导出、邀请、成员状态和角色操作文字都在自身边框内居中。
- [x] 管理列表在 375/390 px 下操作区与内容垂直居中，不遮挡长地点名、地址和原因。
- [x] 320 px 下管理列表操作区可以换到下一行，按钮不横向溢出，文字不裁切。
- [x] 安装区的“安装到设备”按钮在浏览器可安装状态下保持固定高度和居中；微信、iOS、已安装等纯说明状态不受影响。
- [x] 业务交互、权限和文案与 V1.4 保持一致，没有新增数据库请求或第三方资源请求。

## 6. 回归与结论

- [x] `/`、`/admin` 和受影响控件在桌面宽度下无回归。
- [x] 键盘焦点环清晰，disabled 状态仍可辨识且不改变控件布局。
- [x] Network 中按钮资产来自 `/images/v1-4-1/place-management-button.png`，没有新的外部图片请求。
- [x] 通过 / 有条件通过 / 不通过：`通过`
- 验收人：`项目负责人`
- 验收日期：`2026-08-05`
- 遗留问题、复现步骤和截图：

```text
无遗留问题。
```
